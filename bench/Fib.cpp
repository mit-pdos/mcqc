// Autogenerated by MCQC
#pragma once
#include "nat.hpp"
#include "io.hpp"
#include "show.hpp"

using namespace Nat;
using namespace Show;
using namespace Io;

nat fib(nat n) {
  return match(n,
    [=]() { return (nat)1; },
    [=](auto sm) { return match(sm,
      [=]() { return (nat)1; },
      [=](auto m) { return add(fib(m), fib(sm)); }); });
}

int main(int argc, char** argv) {
  auto test = (nat)atoi(argv[1]);
  print(string("Fibonacci"));
  print(show(fib(test)));
  return 0;
}

