// Autogenerated by MCQC
#pragma once
#include "nat.hpp"
#include "proc.hpp"
#include "show.hpp"

using namespace Nat;
using namespace Show;
using namespace Proc;

nat fact(nat n) {
  return match(n,
    [=]() { return (nat)1; },
    [=](auto sm) { return mul(n, fact(sm)); });
}

int main(int argc, char** argv) {
  auto test = (nat)atoi(argv[1]);
  print(string("Factorial"));
  print(show(fact(test)));
  return 0;
}

